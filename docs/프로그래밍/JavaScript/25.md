---
layout: default
title: 호이스팅 (Hoisting)
has_children: false
published_date: 2023-08-26
last_modified_date: 2023-11-03
nav_order: 14
grand_parent: 프로그래밍
parent: JavaScript
permalink: '25'
---
## 선행

- [실행 컨텍스트 (Execute Context)](https://devshjeon.github.io/23)

## 요약

- 호이스팅은 실행 컨텍스트가 생성되는 단계에서 식별자 정보를 environment record에 저장하는 과정입니다.
- 호이스팅은 변수, 함수, 모듈에서 발생합니다.
- 호이스팅은 선언에만 관련이 있습니다.

## 호이스팅 (Hoisting)이란?


호이스팅이란 실행 컨텍스트가 생성되는 단계에서 현재 컨텍스트의 식별자 정보를 lexical environment의 environment record에 저장하는 과정을 의미합니다.


호이스팅은 변수, 함수, 모듈에서 발생하고 이들의 선언에만 관련이 있습니다.


이때, 변수, 함수를 어떻게 선언하느냐에 따라서 environment record에 저장되는 방식이 달라집니다.


### 변수


변수를 선언하는 방식에는 `var`, `let`, `const`가 있습니다.


`var`로 생성한 변수는 environment record에 저장될 때, 선언과 함께 `undefined`로 초기화를 진행합니다.


{% raw %}
```javascript
console.log(x); // undefined
var x = 1;
```
{% endraw %}


아래 예제는 호이스팅이 아닌 `var`의 스코프 영역과 관련이 있습니다.


{% raw %}
```javascript
{
  var x = 1;
}
console.log(x); // 1
```
{% endraw %}


반면, `let`, `const`로 생성한 변수는 선언된 값을 저장하지만 초기화를 진행하지 않습니다. (이때, TDZ 단계로 진입하여 이 단계에서 참조 시 참조 에러가 발생합니다)


{% raw %}
```javascript
const x = 1;
{
  console.log(x); // ReferenceError
  const x = 2;
}
```
{% endraw %}


### 함수


함수의 경우 **선언식**, **표현식**으로 나눌 수 있는데, 선언식의 경우 함수 그대로 environment record에 저장됩니다.


따라서 함수를 선언하기 이전에 사용할 수 있습니다.


{% raw %}
```javascript
foo(); // bar
function foo() {
  console.log('bar');
}
```
{% endraw %}


반면, 표현식의 경우 함수가 변수에 할당되기 때문에 함수가 저장되지 않고 변수 호이스팅 과정이 진행됩니다.


{% raw %}
```javascript
foo(); // Uncaught TypeError: foo is not a function
var foo = function () {
  console.log('bar');
}
```
{% endraw %}


### 모듈


모듈도 호이스팅이 진행됩니다.


{% raw %}
```javascript
const myCanvas = new Canvas("myCanvas", document.body, 480, 320);
myCanvas.create();
import { Canvas } from "./modules/canvas.js";
myCanvas.createReportList();
```
{% endraw %}


## Reference


[https://developer.mozilla.org/en-US/docs/Glossary/Hoisting](https://developer.mozilla.org/en-US/docs/Glossary/Hoisting)


[https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Modules#import_declarations_are_hoisted](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Modules#import_declarations_are_hoisted)

